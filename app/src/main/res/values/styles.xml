<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--color of home(back) button-->
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="SignIn" parent="AppTheme">
        <item name="colorAccent">@color/colorAccent2</item>
        <item name="android:textColor">@color/signInText</item>
        <item name="android:textColorHint">@color/signInTextHint</item>
    </style>

    <style name="PrefTheme" parent="AppTheme">
        <item name="android:textSize">@dimen/textSizeStat</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
